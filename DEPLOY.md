# 部署指南

## 准备图标

在部署前，需要创建 `icon.png` 文件（512x512 像素）：

### 方法 1：使用浏览器生成
1. 在浏览器中打开 `generate-icon.html`
2. 右键点击显示的图标
3. 选择"图片另存为"，保存为 `icon.png`

### 方法 2：使用 ImageMagick
```bash
brew install imagemagick
./create-icon.sh
```

### 方法 3：使用在线工具
1. 打开 icon.svg
2. 使用在线 SVG 转 PNG 工具（如 cloudconvert.com）
3. 转换为 512x512 的 PNG
4. 保存为 icon.png

## 部署到 Vercel

### 使用 Vercel CLI
```bash
# 安装 Vercel CLI
npm i -g vercel

# 登录
vercel login

# 部署
vercel

# 部署到生产环境
vercel --prod
```

### 使用 Vercel 网站
1. 访问 https://vercel.com
2. 点击 "New Project"
3. 导入此 Git 仓库（或上传文件夹）
4. 点击 "Deploy"

## 添加到 iOS 主屏幕

1. 在 iPhone 的 Safari 浏览器中打开部署的网址
2. 点击底部的"分享"按钮（方框带向上箭头）
3. 向下滚动，选择"添加到主屏幕"
4. 点击"添加"

应用会以全屏模式运行，看起来就像系统自带的计算器应用！

## 魔术使用技巧

1. **提前准备**：在表演前，先输入目标数字（如当天日期时间），然后长按右上角设置
2. **自然操作**：让观众看到你正常使用计算器，不要让他们注意到右上角的点击
3. **时机把握**：在观众说完两个数字并得到结果后，自然地"思考"一下，此时快速点击右上角
4. **最终揭晓**：按等号时可以稍作停顿，增加戏剧效果

## 注意事项

- 右上角的秘密区域大小为 60x60 像素，位置固定
- 长按（1秒）设置目标数字，快速点击计算差值
- 每次表演需要重新设置目标数字
- 建议在表演前多练习几次，确保操作流畅
